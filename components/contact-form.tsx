'use client';
import {useState} from 'react';
export default function ContactForm(){const [busy,setBusy]=useState(false),[message,setMessage]=useState('');return <form className="stack" onSubmit={async e=>{e.preventDefault();const form=e.currentTarget;setBusy(true);setMessage('');const data=Object.fromEntries(new FormData(form));try{const r=await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const d=await r.json();if(!r.ok)throw Error(d.error);setMessage('Đã nhận yêu cầu. Chúng tôi sẽ liên hệ với bạn.');form.reset();}catch(e){setMessage(e instanceof Error?e.message:'Chưa gửi được yêu cầu.');}finally{setBusy(false);}}}><input name="name" aria-label="Họ và tên" placeholder="Họ và tên" required minLength={2} maxLength={100}/><input name="phone" aria-label="Số điện thoại tư vấn" placeholder="Số điện thoại" type="tel" required minLength={8} maxLength={20}/><input name="email" aria-label="Email tư vấn" placeholder="Email (không bắt buộc)" type="email" maxLength={254}/><input name="website" tabIndex={-1} autoComplete="off" aria-hidden="true" style={{display:'none'}}/><button className="button gold" disabled={busy}>{busy?'Đang gửi…':'Đăng ký tư vấn'}</button><small>Bằng việc gửi yêu cầu, bạn đồng ý để chúng tôi liên hệ tư vấn.</small>{message&&<p role="status">{message}</p>}</form>;}
