services:
  alpha-hub:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    env_file: .env.local
    environment:
      ALPHA_DATA_DIR: /app/data
      ALPHA_SECURE_COOKIE: "true"
    volumes:
      - ./data:/app/data
