import {sqliteTable,text,integer,primaryKey,index} from 'drizzle-orm/sqlite-core';
export const records=sqliteTable('records',{owner:text('owner').notNull(),kind:text('kind').notNull(),id:text('id').notNull(),payload:text('payload').notNull(),updated:integer('updated').notNull()},t=>[primaryKey({columns:[t.owner,t.kind,t.id]})]);
export const reservations=sqliteTable('reservations',{id:text('id').primaryKey(),owner:text('owner').notNull(),unit_id:text('unit_id').notNull(),customer_id:text('customer_id').notNull(),note:text('note').notNull(),status:text('status').notNull(),expires_at:integer('expires_at').notNull(),created_at:integer('created_at').notNull()},t=>[index('idx_reservations_owner_unit').on(t.owner,t.unit_id,t.status)]);
export const files=sqliteTable('files',{id:text('id').primaryKey(),owner:text('owner').notNull(),project_id:text('project_id').notNull(),kind:text('kind').notNull(),name:text('name').notNull(),mime:text('mime').notNull(),object_key:text('object_key').notNull()},t=>[index('idx_files_owner').on(t.owner)]);
